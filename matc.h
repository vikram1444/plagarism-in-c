float matc(float a,float b){         //This function takes the input and manipulate ,return the calculated result
    float match;              //taking match variable as result
    match = (a/b) * 100;        //result
    return match;
}
